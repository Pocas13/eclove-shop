import { getServerSession } from "next-auth";import { authOptions } from "@/lib/auth";import { contaAprovada } from "@/lib/acesso";import { prisma } from "@/lib/prisma";import Link from "next/link";
export const dynamic="force-dynamic";
export default async function Catalogos(){const session=await getServerSession(authOptions);const ok=contaAprovada(session);const catalogos=ok?await prisma.catalogo.findMany({where:{ativo:true},orderBy:{createdAt:"desc"}}):[];return <main className="container-site py-16"><p className="eyebrow">Biblioteca Eclove</p><h1 className="mt-3 font-display text-5xl font-bold">Catálogos profissionais</h1>{!ok?<div className="login-gate mt-10"><b>Acesso sujeito a validação</b><p>Os catálogos comerciais ficam disponíveis apenas a contas aprovadas.</p><div><Link href="/entrar" className="btn-primary">Entrar</Link><Link href="/registo" className="btn-secondary">Pedir acesso</Link></div></div>:catalogos.length?<div className="mt-12 grid gap-5 md:grid-cols-3">{catalogos.map(c=><article key={c.id} className="border border-black/10 bg-white p-6"><span className="text-xs uppercase tracking-widest">PDF</span><h2 className="mt-8 font-display text-2xl font-bold">{c.titulo}</h2><p className="mt-3 text-sm text-black/55">{c.descricao}</p><a href={c.ficheiroUrl} target="_blank" rel="noreferrer" className="mt-8 inline-block border-b border-black">Descarregar catálogo →</a></article>)}</div>:<p className="mt-10 text-black/55">Os primeiros catálogos serão publicados aqui.</p>}</main>}
