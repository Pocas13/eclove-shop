import { getServerSession } from "next-auth";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { redirect } from "next/navigation";
export const dynamic="force-dynamic";
export default async function Listas(){const s=await getServerSession(authOptions);if(!s?.user)redirect('/entrar');const listas=await prisma.listaProjeto.findMany({where:{userId:(s.user as any).id},include:{_count:{select:{itens:true}}},orderBy:{updatedAt:'desc'}});return <main className="container-site py-16"><p className="eyebrow">Área reservada</p><h1 className="mt-3 font-display text-5xl font-bold">Listas de clientes e projetos</h1><p className="mt-4 max-w-2xl text-black/55">Organize seleções de produtos por obra, espaço ou cliente. A estrutura está preparada para orçamento e acompanhamento comercial.</p><div className="mt-10 grid gap-4 md:grid-cols-3">{listas.map(l=><article key={l.id} className="border border-black/10 bg-white p-6"><span className="text-xs uppercase tracking-wider">{l._count.itens} artigos</span><h2 className="mt-5 font-display text-2xl font-bold">{l.nome}</h2><p className="mt-2 text-sm text-black/50">{l.cliente||'Projeto interno'}</p></article>)}<article className="grid min-h-52 place-items-center border border-dashed border-black/30 p-6 text-center"><div><b>Nova lista</b><p className="mt-2 text-sm text-black/50">Criação pelo seletor de produtos na próxima fase operacional.</p></div></article></div></main>}
