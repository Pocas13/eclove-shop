import { prisma } from "@/lib/prisma";import { formatarEuros } from "@/lib/precos";import Link from "next/link";export const dynamic="force-dynamic";
export default async function AdminClientesPage(){const clientes=await prisma.user.findMany({where:{role:"PROFISSIONAL",profissionalEstado:"APROVADO"},include:{encomendas:{select:{total:true,estado:true}},_count:{select:{favoritos:true,listas:true,historico:true}}},orderBy:{createdAt:"desc"}});return <div><p className="eyebrow">CRM comercial</p><h1 className="mt-3 font-display text-4xl font-bold">Clientes validados</h1><p className="mt-3 text-sm text-black/50">Ficha, atividade, encomendas, faturas, listas e histórico de acompanhamento.</p><div className="table-wrap mt-8"><table className="w-full text-sm"><thead><tr className="bg-calcario-100 text-left"><th className="p-3">Cliente</th><th className="p-3">Contacto</th><th className="p-3">Atividade</th><th className="p-3">Compras</th><th className="p-3"></th></tr></thead><tbody>{clientes.map(c=>{const total=c.encomendas.filter(e=>e.estado!=="CANCELADA").reduce((a,e)=>a+Number(e.total),0);return <tr key={c.id} className="border-t"><td className="p-3"><b>{c.empresaNome}</b><small className="block text-black/45">{c.nome} · NIF {c.nif}</small></td><td className="p-3">{c.email}<small className="block text-black/45">{c.telefone}</small></td><td className="p-3">{c._count.listas} listas · {c._count.favoritos} favoritos</td><td className="p-3">{c.encomendas.length} · {formatarEuros(total)}</td><td className="p-3 text-right"><Link href={`/admin/clientes/${c.id}`} className="border-b border-black">Abrir ficha →</Link></td></tr>})}</tbody></table></div></div>}
